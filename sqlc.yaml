version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "mysql"
    gen:
      go:
        package: "ntpdb"
        out: "ntpdb"
        emit_json_tags: true
        # emit_all_enum_values: true
        # overrides:
        #   - column: "x.avg_rtt"
        #     go_type: "database/sql.NullFloat64"
